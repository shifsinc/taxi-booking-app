<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<entry key="authentication.credentials.validation">
		select count(*) from
		users where upper(username) = upper(:username) and password = :password
	</entry>
</properties>
